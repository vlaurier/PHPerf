// Command phperf démarre l'interface web de PHPerf.
//
// En production, ce binaire sert l'interface permettant d'explorer les
// profils, de consulter les findings priorisés et de gérer le triage
// (masquage). En CI, préférer phperf-ci.
package main

import "fmt"

func main() {
	fmt.Println("phperf web UI — à implémenter")
}
